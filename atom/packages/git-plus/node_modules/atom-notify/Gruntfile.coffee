module.exports = (grunt) ->
  grunt.initConfig
    coffee:
      compile:
        files:
          'dist/index.js': 'notify.coffee'

  grunt.loadNpmTasks 'grunt-contrib-coffee'
  grunt.registerTask 'default', ['coffee'] 
